rules_version = '2';
// ⚠️ 這份規則目前不是實際的存取控制邊界，真正的驗證在 Express middleware (OdyShareSync)
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
