rules_version = '2';
// 🛡️ OdyShare Fortress Rules - Fully server-authoritative lockdown
service cloud.firestore {
  match /databases/{database}/documents {
    // Lock down everything. Access is permitted only via server-side Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
